#include "checkerFT.h"

boolean CheckerFT_Node_isValid(NodeFT_T oNNode) {
    return TRUE;
}

boolean CheckerFT_isValid(boolean bIsInitialized,
                          NodeFT_T oNRoot,
                          size_t ulCount) {
    return TRUE;
}